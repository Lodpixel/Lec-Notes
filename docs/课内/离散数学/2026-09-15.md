# 离散数学｜2026 年 9 月 15 日课程转录（可读校订稿）

来源：上海交通大学 OC 课程视频系统字幕。

说明：本版不是逐字字幕，而是按照课堂顺序整理出的可读校订稿。保留教师的讲授逻辑、主要例子、公式和时间点；删除明显的环境噪声、无意义的重复口头语及与课程无关的临时对话；同时统一修正语音识别造成的专有名词、数学术语、标点和公式错误。本版不额外扩展为知识点总结。

日期：2026-09-15

## 2026 年 9 月 15 日｜第 1 节回放

### 课程定位与学习目标

**05:51**

这学期的离散数学由我和林泽宇老师共同授课。我是王兆国，来自软件学院的 IPD 实验室，主要从事系统方向的研究。

大家可能会好奇，为什么由我们实验室来讲离散数学？这门课并不是一门与计算机实践完全割裂的数学课。我们希望它能够回答两个与计算机科学直接相关的问题：

1. 计算机能够解决什么样的问题？
2. 我们如何知道一个程序是正确的？

这门课的重点不是让大家记住许多彼此孤立的数学结论，而是让大家在以后做项目、写程序或阅读论文时，能够把离散数学中的工具用起来。

**06:21**

软件工程方向的离散数学和大平台课程会有一些不同。软件工程的课程体系与编程、软件系统、操作系统等课程联系得比较紧，因此这门课会围绕软件工程和计算机科学中的问题展开。其他专业的同学如果选修，需要注意后续可能要补充较多的相关背景知识。

这门课面向软件工程专业的大二学生。把课程放在大二，是为了让大家能够和同期的编程课程配合起来学习。

**08:03**

我一直强调，这门课是为计算机科学服务的。离散数学和计算机之间通常不是“一学完就能直接写出某个程序”的关系，而是提供一些基础工具：当你遇到一个具体问题时，可能会用到其中某一个离散结构、某一种逻辑表达或某一种证明方法。

更重要的是，过了一年、两年甚至更长时间之后，大家仍然能够记得：离散数学为什么是计算机科学的基础，它和程序设计、程序验证之间有什么联系。

### 离散数学与 coding agent

**11:27**

现在大家已经进入大模型和 coding agent 的时代。很多同学已经使用过 Codex、DeepSeek 或其他模型来写代码。编程方式正在发生变化，因此我们也要重新思考：在 coding agent 大量参与软件开发的情况下，为什么还要学习离散数学？

过去，我们通常是先分析问题、设计算法、写代码、运行测试，再根据测试结果修改程序。现在的流程往往变成：

- 人定义问题，并提供上下文；
- coding agent 读取现有代码或创建新的代码；
- agent 编写、修改并运行测试；
- 人负责审查结果，并对最终代码负责。

以前主要是“人写代码、机器执行”；现在逐渐变成“人描述目标、agent 生成代码、人审查和承担责任”。

**17:35**

coding agent 的快速发展已经改变了软件开发生态。它可以在很短时间内生成大量代码，但代码量增加并不等于软件质量提高。真正的问题是：谁来检查这些代码的设计是否正确、算法是否满足目标、实现中是否存在隐藏的错误？

以前我们学习了很多如何开发程序、设计算法和优化系统的课程，但很少有课程专门讨论如何系统地审查大模型生成的代码，以及如何自动检查程序是否满足预期。

离散数学，尤其是数理逻辑，正好提供了这类工具。我们希望把程序的正确性写成数学上可以检查的形式，再利用自动化验证工具去判断它是否成立。

**22:42**

代码规模正在以前所未有的速度增长。大模型可以生成大量代码，但如果没有人检查这些代码的设计和语义，整个软件系统就可能像一座结构不稳的建筑。

测试能够发现某些 bug，却不能保证不存在 bug。一个测试用例通过，只能说明程序在这个输入上表现正确，不能说明程序对所有输入都正确。

因此，我们需要从“测试程序”进一步走向“证明程序”。这也是本课程和 coding agent 的核心联系：未来的瓶颈可能不再是写代码，而是定义正确性、审查代码和验证代码。

**30:08**

程序正确性的讨论很早就出现了。程序员会使用 assertion，在程序运行到某个位置时检查一个条件是否成立。后来，随着软件规模变大，研究者开始发展形式化验证理论，例如霍尔逻辑、时序逻辑等。

这些理论本身并没有失效，但传统方法往往需要人手动完成大量推理，而 coding agent 生成代码的速度远远超过了人工审查的速度。因此，我们需要把数学基础和自动化工具结合起来，让计算机协助完成严格验证。

大模型的基础更多来自连续数学，而程序的结构、状态和正确性验证则大量依赖离散数学。要让 AI 辅助编程形成闭环，需要连续数学和离散数学共同发挥作用。

### 课程结构与教材

**34:23**

本课程主要分为两部分：我负责的部分以数理逻辑为主，另一部分是可计算理论和相关内容。课程会用逻辑与图论等工具回答：如何定义程序应该完成什么，以及如何验证它确实完成了目标。

标准的离散数学通常包括图论、逻辑和集合论。由于本课程面向软件工程，课程内容做了调整：图论的一部分会放到后续高级算法课程中，当前课程更多集中在程序正确性、逻辑推理和自动化验证。

**39:46**

课程会提供配套的 lecture notes。教材方面，逻辑与集合论部分参考学校统一教材；可计算理论部分会提供相应的 PDF 或补充材料。具体每次课使用哪些章节，会在课程网站和 Canvas 上说明。

课程大致分为三部分：

- 逻辑推理，约十周；
- 可计算理论，约五周；
- 应用和实验，约一周。

逻辑与应用部分由我负责，可计算理论部分由林泽宇老师负责。课程会配套 lab，助教和实验团队会帮助大家完成实验任务。

### 考核与课程政策

**47:15**

目前课程考核大致包括：

- 期末考试：约占 50%；
- lab、课堂参与和 homework：约占 30%；
- quiz：约占 20%。

课堂参与通常占比不高，但会参考大家是否参加课堂、是否参与讨论。每节课会布置课后作业，也可能提供预习作业；预习作业主要用于帮助大家准备课程，不一定计入成绩。

课程采用中英文结合的方式：课件和关键术语会使用英文，教材和考试主要使用中文。大家需要熟悉诸如 proposition、proof、specification、precondition、postcondition、solver 等术语。

**49:47**

今年的作业政策会结合 coding agent。大家应当使用课程允许的 agent 完成自己的作业，不能直接使用他人的作业，也不能直接复用他人整理好的 agent 结果。重点不是让 agent 替大家完成学习，而是让大家理解 agent 做了什么、如何检查它的输出，以及如何对结果负责。

如果没有特别说明，作业和实验默认在截止日 23:59 截止，逾期不再自动顺延。每位同学有五天 grace days，用于处理临时情况。grace days 按半天计，逾期多久就扣除相应额度；如果不申请使用，或额度已经用完，逾期作业按课程规定处理。

### 本节小结

**55:03**

这节课主要说明了课程为什么存在、它和计算机以及 coding agent 的关系、课程的结构与考核方式。后面我们会正式进入数理逻辑，从 proof、命题和程序规约开始，逐步学习如何把程序正确性写成可以由计算机检查的形式。

## 2026 年 9 月 15 日｜第 2 节回放

### 课程团队与课程平台

**06:39**

这门课的课程团队包括授课教师、博士生助教、硕士生助教和负责 lab 改革的协作成员。前半部分数理逻辑的问题主要联系负责这一部分的助教；可计算理论部分的问题联系相应助教。具体分工会公布在课程群和 Canvas 上。

课程网站和 Canvas 都会使用。Canvas 适合提交作业和发布正式通知，课程微信群适合进行更及时的答疑和讨论。大家遇到问题时，应当优先查阅课程材料，再在对应平台提问。

### 什么是离散数学

**10:38**

离散数学首先是一门数学。它研究的对象通常是彼此分离、可以逐个讨论的离散对象。对象集合可以是有限的，也可以是可数无限的；除了研究对象本身，还要研究对象之间的关系，以及对象状态如何按照规则发生变化。

这正是离散数学适合描述计算机的原因。温度、时间和距离常常用连续量表示，而程序状态、变量取值、状态机中的状态以及程序执行的步骤，都可以看作离散对象。

例如，一个程序可以有若干种状态；执行一条指令后，从一个状态转移到另一个状态。我们可以用集合表示状态，用关系表示状态之间的转移，再用规则描述状态如何变化。

离散数学中常见的分支包括数论、集合论、逻辑和图论。

- 数论研究整数及其运算和关系；
- 集合论研究对象的集合、子集、集合运算和映射；
- 逻辑研究命题的真假以及命题之间的推理关系；
- 图论用于描述程序结构、网络和状态之间的连接。

这些概念在程序设计中经常出现。例如，程序能够访问哪些文件、接受哪些输入、有哪些可能的状态，实际上都可以用集合来抽象。

### 从逻辑门到程序验证

**19:02**

在数字电路中，信号通常用高电平和低电平表示，可以抽象为 1 和 0。在逻辑中，我们把它们对应为 true 和 false。

常见的逻辑门可以对应命题联结词：

- OR 门对应“或”；
- NOT 门对应“非”；
- AND 门对应“与”。

电路设计完成后，通常还需要优化：用更少的门、更小的面积或更低的成本实现相同功能。芯片制造成本很高，因此不能只凭几个测试输入判断优化前后的电路等价，而需要用形式化方法证明它们对所有输入都有相同的输出。

例如，电路等价问题可以转化为逻辑表达式的等价问题。把高电平映射为 true、低电平映射为 false 后，电路验证就变成了：两个命题表达式是否对所有输入都有相同的真值。

### 从测试走向规约

**24:22**

再看一个程序例子：判断整数 n 是否是 2 的幂。

一种直接的算法是逐位检查 n 的二进制表示，统计其中为 1 的 bit 数量。若只有一个 bit 为 1，则 n 是 2 的幂。另一种更简洁的写法利用性质：

~~~text
n > 0 且 (n & (n - 1)) = 0
~~~

这段代码更短、性能也可能更好，但对第一次阅读的人来说不一定更容易理解。代码越短，不代表证明它正确越容易。

我们当然可以生成很多 test case，例如测试 0、1、2、4、8 以及一些不是 2 的幂的数。但无论测试多少组有限输入，都不能穷尽所有整数，也不能由此保证程序对所有输入都正确。

**30:43**

因此，验证程序之前必须先明确“正确”是什么意思。我们需要先给程序写出一个形式化的正确性描述，这个描述叫作 specification，中文通常译为“规约”。

以计算 1 到 n 的和为例。程序的目标是返回：

~~~text
1 + 2 + ... + n = n(n + 1) / 2
~~~

如果输入要求为自然数，可以把前置条件写成：

~~~text
n ≥ 0
~~~

把程序执行后的要求写成后置条件：

~~~text
result = n(n + 1) / 2
~~~

规约通常包含两个部分：

- 前置条件（precondition）：函数执行前输入或状态必须满足的条件；
- 后置条件（postcondition）：如果前置条件成立，函数执行结束后必须满足的条件。

程序验证的目标，就是证明：当输入满足前置条件时，程序的执行一定能推出后置条件。

**38:41**

所以，程序验证大致分成两步：

1. 把程序的正确性写成规约；
2. 把程序和规约转换成数学逻辑表达式，再检查这个表达式是否成立。

命题逻辑是第一步基础工具。之后还会学习更强的谓词逻辑，以表达变量、函数和量词。计算机负责求解这些逻辑表达式的工具叫 solver；我们还会讨论如何使用 agent 优化 solver。

### 自动化验证的边界

**42:09**

自动化验证并不能解决所有问题。对于带有复杂循环的程序，不存在一个算法能够对所有程序都自动完成验证。这类问题在理论上可能是不可判定的。

因此，实际验证往往需要自动化和人工结合：自动化工具负责处理可判定、结构清晰的部分，人负责提供循环不变量、辅助引理或公理系统。课程后面的实验会涉及自动化验证和半自动化验证。

本课程会安排多个 lab。大家会得到一定的模型和 token 额度，在限定条件下使用 agent 完成实验，并观察 agent 如何生成、修改和验证程序。实验评价的重点是理解验证过程，而不只是把测试用例跑通。

### 下节课预告

**46:11**

下一节课会先讲 proof。大家高中阶段都接触过数学证明，我们会从简单的数学证明开始，再介绍 Lean 这类形式化证明语言，看看计算机如何检查证明是否正确。

Lean 本身不是大模型，而是一种能够检查形式化证明的语言和工具。它不负责替人凭空发现证明，但可以对已经写出的证明逐步检查，并指出不成立的位置。Lean 不作为本课程的主要考试内容，主要用于帮助大家建立直观认识。

随后我们会正式进入命题逻辑：什么是命题、什么是命题联结词，以及如何用命题对程序进行建模。
